name: sync_backend
description: Self-hosted sync backend for Sheet Music Reader.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.6
  path: ^1.9.1
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: 3.1.6
  sync_protocol:
    path: ../../packages/sync_protocol

dev_dependencies:
  lints: 6.1.0
